{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (OSX)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/mssql",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}